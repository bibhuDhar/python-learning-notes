# Numpy(Numerical Python)
Helps to do mathemetical operations faster
Core structure -> Nd array
In list we can store different kind of things,List is slow beacause we can store mixed data,
Nd array works like the array in c,blocks in nd array are continuos secondly it store one type of data,list is scattered so we have to access the data by using pointer but in nd array we don't need pointer
vectorization->in our cpu have a feature simd(single instruction multiple data),by using this feature we can run a operation on all the items of an array
Numpy is created for mathematical works,statics works also linear algebra
